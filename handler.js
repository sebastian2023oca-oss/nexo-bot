import ping from './src/ping.js'
import saludo from './src/saludo.js'
import menu from './src/menu.js'
import chiste from './src/chiste.js'
import autoRegistrar from './src/autoRegistrar.js'
import addbot from './src/addbot.js'
import solicitudes from './src/solicitudes.js'
import aceptar from './src/aceptar.js'
import rechazar from './src/rechazar.js'
import rules from './src/rules.js'

import perfil from './src/perfil.js'
import nivel from './src/nivel.js'
import xp from './src/xp.js'
import stats from './src/stats.js'
import insignias from './src/insignias.js'
import rank from './src/rank.js'
import top from './src/top.js'
import setbio from './src/setbio.js'
import setstatus from './src/setstatus.js'
import setname from './src/setname.js'
import resetperfil from './src/resetperfil.js'
import reputacion from './src/reputacion.js'
import followers from './src/followers.js'
import follow from './src/follow.js'
import block from './src/block.js'
import id from './src/id.js'
import avatar from './src/avatar.js'

import balance from './src/balance.js'
import banco from './src/banco.js'
import saldo from './src/saldo.js'
import depositar from './src/depositar.js'
import retirar from './src/retirar.js'
import work from './src/work.js'
import daily from './src/daily.js'
import interes from './src/interes.js'
import minar from './src/minar.js'
import pescar from './src/pescar.js'
import cazar from './src/cazar.js'
import recolectar from './src/recolectar.js'
import negocio from './src/negocio.js'
import repartir from './src/repartir.js'
import beg from './src/beg.js'
import transferir from './src/transferir.js'
import pagar from './src/pagar.js'
import invertir from './src/invertir.js'
import rentabilidad from './src/rentabilidad.js'
import prestamo from './src/prestamo.js'
import pagar_prestamo from './src/pagar_prestamo.js'
import aceptarprestamo from './src/aceptarprestamo.js'
import rechazarprestamo from './src/rechazarprestamo.js'
import deuda from './src/deuda.js'
import apostar from './src/apostar.js'
import loteria from './src/loteria.js'
import ruleta from './src/ruleta.js'
import robar from './src/robar.js'
import robar_item from './src/robar_item.js'
import hack from './src/hack.js'
import coinflip from './src/coinflip.js'
import slots from './src/slots.js'
import blackjack from './src/blackjack.js'
import shopcoins from './src/shopcoins.js'
import comprar from './src/comprar.js'
import topbank from './src/topbank.js'
import topmoney from './src/topmoney.js'
import expedicion from './src/expedicion.js'
import aventura from './src/aventura.js'

import tienda from './src/tienda.js'
import precio from './src/precio.js'
import stock from './src/stock.js'
import inventario from './src/inventario.js'
import listar from './src/listar.js'
import usar from './src/usar.js'
import equipar from './src/equipar.js'
import desequipar from './src/desequipar.js'
import vender from './src/vender.js'
import regalar from './src/regalar.js'
import mejorar from './src/mejorar.js'
import bodega from './src/bodega.js'
import almacenar from './src/almacenar.js'
import sacar from './src/sacar.js'
import historico from './src/historico.js'

import trivia from './src/trivia.js'
import adivina from './src/adivina.js'
import hangman from './src/hangman.js'
import quiz from './src/quiz.js'
import mathgame from './src/mathgame.js'
import dado from './src/dado.js'
import suerte from './src/suerte.js'
import luck from './src/luck.js'
import ppt from './src/ppt.js'
import duelo from './src/duelo.js'
import speedtype from './src/speedtype.js'
import adivinanumero from './src/adivinanumero.js'
import riddle from './src/riddle.js'
import pattern from './src/pattern.js'
import memory from './src/memory.js'
import bandera from './src/bandera.js'

// Menu 5: Casino / Apuestas — Suerte y Riesgo
import caja from './src/caja.js'
import doblenada from './src/doblenada.js'
import escalera from './src/escalera.js'
import tesoro from './src/tesoro.js'
import misterio from './src/misterio.js'
import sobrevive from './src/sobrevive.js'
import jackpot from './src/jackpot.js'
import raspaygana from './src/raspaygana.js'

// Menu 5: Casino / Apuestas — PvP
import retar from './src/retar.js'
import aceptarreto from './src/aceptarreto.js'
import cancelarreto from './src/cancelarreto.js'
import coinflipvs from './src/coinflipvs.js'
import blackjackvs from './src/blackjackvs.js'
import ruletavs from './src/ruletavs.js'
import allin from './src/allin.js'
import subasta from './src/subasta.js'
import dueloapuesta from './src/dueloapuesta.js'
import venganza from './src/venganza.js'

// Menu 5: Casino / Apuestas — Competitivo
import rankingcasino from './src/rankingcasino.js'
import topapostadores from './src/topapostadores.js'
import topganadores from './src/topganadores.js'
import topperdedores from './src/topperdedores.js'
import racha from './src/racha.js'
import estadisticascasino from './src/estadisticascasino.js'
import historialapuestas from './src/historialapuestas.js'

// Menu 5: Casino / Apuestas — Eventos especiales
import apostagrupal from './src/apostagrupal.js'
import guerra from './src/guerra.js'
import torneo from './src/torneo.js'
import reycasino from './src/reycasino.js'
import pozomundial from './src/pozomundial.js'
import ultimohombre from './src/ultimohombre.js'
import caceria from './src/caceria.js'
import atraparcaceria from './src/atraparcaceria.js'

// Owners
import add from './src/add.js'
import addowner from './src/addowner.js'
import addstock from './src/addstock.js'
import addvip from './src/addvip.js'
import addnegocio from './src/addnegocio.js'
import adjustprices from './src/adjustprices.js'
import aviso from './src/aviso.js'
import backup from './src/backup.js'
import banuser from './src/banuser.js'
import coronar from './src/coronar.js'
import delowner from './src/delowner.js'
import demoteall from './src/demoteall.js'
import drop from './src/drop.js'
import eventocm from './src/eventocm.js'
import getcommand from './src/getcommand.js'
import mute from './src/mute.js'
import nuke from './src/nuke.js'
import ordenartienda from './src/ordenartienda.js'
import nexobotlogs from './src/nexobotlogs.js'
import penalizar from './src/penalizar.js'
import reiniciar from './src/reiniciar.js'
import reply from './src/reply.js'
import resetstock from './src/resetstock.js'
import reunion from './src/reunion.js'
import unbanuser from './src/unbanuser.js'
import unmute from './src/unmute.js'
import makecode from './src/makecode.js'
import viewcodes from './src/viewcodes.js'
import canjear from './src/canjear.js'
import cagar from './src/cagar.js'
import violar from './src/violar.js'
import impuestos from './src/impuestos.js'
import inflacion from './src/inflacion.js'
import restablecimiento from './src/restablecimiento.js'

import db from './src/db.js'
import { esOwner, inicializarOwners } from './src/owners.js'
import { asegurarTablasCasino } from './src/casinoUtils.js'
import { OWNERS_JID as OWNERS_JID_GLOBAL } from './src/config.js'
import {
    inicializarAntibaneo,
    verificarFlood,
    verificarSpamInterno,
    obtenerStatsAntibaneo,
} from './src/antibaneo.js'
import {
    warmupCache,
    iniciarLimpiezaCache,
    invalidarCache,
    obtenerStatsCache,
} from './src/cache.js'
import {
    inicializarOps,
    registrarError,
    registrarActividad,
    obtenerEstadoSistema,
    obtenerResumenActividad,
} from './src/ops.js'
import {
    asegurarTablaConfig,
    activarMantenimiento,
    desactivarMantenimiento,
    estaEnMantenimiento,
    activarEmergencia,
    desactivarEmergencia,
    activarApagadoSeguro,
    estaEnApagadoSeguro,
    activarRampa,
    desactivarRampa,
    verificarRampa,
    estadoRampa,
} from './src/mantenimiento.js'
import {
    asegurarTablasReputacionGrupos,
    registrarIncidente,
    registrarActividadSana,
    obtenerReputacion,
    evaluarTodosLosGrupos,
} from './src/reputacionGrupos.js'

// Inicializar sistemas al arrancar
await inicializarOwners()
await asegurarTablasCasino()
await warmupCache()
iniciarLimpiezaCache()
await asegurarTablaConfig()
await asegurarTablasReputacionGrupos()

// Comandos que modifican el perfil de un usuario y por lo tanto deben
// invalidar su entrada en caché para que el próximo .perfil muestre
// el dato fresco en vez del cacheado (hasta 20s de antigüedad).
const COMANDOS_QUE_INVALIDAN_PERFIL = new Set([
    'setbio', 'setname', 'setstatus', 'resetperfil',
    'addvip', 'addvip-ultra', 'addnegocio', 'addnegocio-ultra',
    'add', 'penalizar', 'equipar', 'desequipar', 'comprar'
])

// Verificar VIP/Negocio expirados cada 5 minutos
async function verificarExpiraciones(sock) {
    try {
        const [vipsExpirados] = await db.execute(
            'SELECT jid, vip_tipo FROM usuarios WHERE vip = 1 AND vip_expira IS NOT NULL AND vip_expira < NOW()'
        )
        for (const u of vipsExpirados) {
            await db.execute('UPDATE usuarios SET vip = 0, vip_tipo = NULL, vip_expira = NULL WHERE jid = ?', [u.jid])
            try {
                await sock.sendMessage(u.jid, {
                    text: `⏰ *Tu membresía VIP ${(u.vip_tipo || 'normal').toUpperCase()} ha expirado.*\n\nSi deseas renovarla escribe *.buyvip* para ver las opciones. 👑`
                })
            } catch {}
        }

        const [negociosExpirados] = await db.execute(
            'SELECT jid, neg_tipo FROM usuarios WHERE negocio = 1 AND neg_expira IS NOT NULL AND neg_expira < NOW()'
        )
        for (const u of negociosExpirados) {
            await db.execute('UPDATE usuarios SET negocio = 0, neg_tipo = NULL, neg_expira = NULL WHERE jid = ?', [u.jid])
            try {
                await sock.sendMessage(u.jid, {
                    text: `⏰ *Tu Plan Negocio ${(u.neg_tipo || 'normal').toUpperCase()} ha expirado.*\n\nSi deseas renovarlo escribe *.buynegocio* para ver las opciones. 🏢`
                })
            } catch {}
        }
    } catch {}
}

const PREFIJO = '.'

const COMANDOS_OWNERS = [
    'add','addowner','addstock','addvip','addvip-ultra','addnegocio','addnegocio-ultra',
    'adjustprices','aviso','backup','banuser','coronar','delowner','demoteall',
    'drop','eventocm','getcommand','mute','nuke','ordenartienda','nexobotlogs',
    'penalizar','reiniciar','reply','resetstock','reunion','unbanuser','unmute',
    'makecode','viewcodes','cagar','violar','antibaneostats','cachestats',
    'statussistema','resumenactividad','mantenimiento','emergencia','apagar','activarrampa',
    'repgrupo','reportargrupo','restablecimiento','impuestos','inflacion',
]

const comandos = {
    ping, saludo, menu, chiste, addbot, solicitudes, aceptar, rechazar,
    perfil, nivel, xp, stats, insignias, rank, top,
    setbio, setstatus, setname, resetperfil, reputacion,
    followers, follow, block, id, avatar,
    balance, banco, saldo, depositar, retirar,
    work, daily, interes, minar, pescar, cazar, recolectar, negocio, repartir, beg,
    transferir, pagar,
    invertir, rentabilidad, prestamo,
    'pagar_prestamo': pagar_prestamo,
    aceptarprestamo, rechazarprestamo, deuda, apostar,
    loteria, ruleta, robar,
    'robar_item': robar_item,
    hack, coinflip, slots, blackjack,
    shopcoins, comprar, topbank, topmoney,
    expedicion, aventura,
    tienda, precio, stock, inventario, listar, usar, equipar, desequipar,
    vender, regalar, mejorar, bodega, almacenar, sacar, historico,
    trivia, adivina, hangman, quiz, mathgame,
    dado, suerte, luck,
    ppt, duelo, speedtype,
    adivinanumero, riddle, pattern, memory,
    bandera, rules,
    canjear,

    // Menu 5: Suerte y Riesgo
    caja, doblenada, escalera, tesoro, misterio, sobrevive, jackpot, raspaygana,

    // Menu 5: PvP
    retar, aceptarreto, cancelarreto, coinflipvs, blackjackvs, ruletavs, allin,
    subasta, dueloapuesta, venganza,

    // Menu 5: Competitivo
    rankingcasino, topapostadores, topganadores, topperdedores, racha,
    estadisticascasino, historialapuestas,

    // Menu 5: Eventos especiales
    apostagrupal, guerra, torneo, reycasino, pozomundial, ultimohombre,
    caceria, atraparcaceria,

    // Owners
    add, addowner, addstock,
    'addvip': { ejecutar: (s,m,a) => addvip.ejecutar(s,m,a,false) },
    'addvip-ultra': { ejecutar: (s,m,a) => addvip.ejecutar(s,m,a,true) },
    'addnegocio': { ejecutar: (s,m,a) => addnegocio.ejecutar(s,m,a,false) },
    'addnegocio-ultra': { ejecutar: (s,m,a) => addnegocio.ejecutar(s,m,a,true) },
    adjustprices, aviso, backup, banuser, coronar, delowner, demoteall,
    drop, eventocm, getcommand, mute, nuke, ordenartienda, nexobotlogs,
    penalizar, reiniciar, reply, resetstock, reunion, unbanuser, unmute,
    makecode, viewcodes, cagar, violar, restablecimiento, impuestos, inflacion,

    // Antibaneo: comando owner para ver el panel de estadísticas internas
    'antibaneostats': {
        async ejecutar(sock, mensaje) {
            const jid = mensaje.key.remoteJid
            const s = obtenerStatsAntibaneo()
            await sock.sendMessage(jid, {
                text: `🛡️ *ANTI-BANEO — ESTADÍSTICAS*\n\n` +
                      `⏱️ *Uptime:* ${s.uptimeMinutos} min\n` +
                      `📤 *Mensajes enviados:* ${s.mensajesEnviados}\n` +
                      `🚫 *Bloqueados por flood:* ${s.mensajesBloqueadosFlood}\n` +
                      `🔁 *Bloqueados por spam repetido:* ${s.mensajesBloqueadosSpam}\n` +
                      `👤 *Usuarios en ventana activa:* ${s.usuariosEnVentana}\n` +
                      `👥 *Grupos en ventana activa:* ${s.gruposEnVentana}\n` +
                      `⛔ *Usuarios bloqueados ahora:* ${s.usuariosBloqueadosAhora}\n` +
                      `📨 *Subcolas pendientes:* ${s.subcolasPendientes}`
            }, { quoted: mensaje })
        }
    },

    // Caché: comando owner para ver hit-rate y entradas activas
    'cachestats': {
        async ejecutar(sock, mensaje) {
            const jid = mensaje.key.remoteJid
            const c = obtenerStatsCache()
            await sock.sendMessage(jid, {
                text: `🗃️ *CACHÉ — ESTADÍSTICAS*\n\n` +
                      `✅ *Hits:* ${c.hits}\n` +
                      `❌ *Misses:* ${c.misses}\n` +
                      `📈 *Hit rate:* ${c.hitRatePorcentaje}%\n` +
                      `🧹 *Invalidaciones:* ${c.invalidaciones}\n` +
                      `📦 *Entradas activas:* ${c.entradasActivas}`
            }, { quoted: mensaje })
        }
    },

    // Ops: estado del sistema (RAM/CPU/BD)
    'statussistema': {
        async ejecutar(sock, mensaje) {
            const jid = mensaje.key.remoteJid
            const s = obtenerEstadoSistema()
            await sock.sendMessage(jid, {
                text: `🩺 *ESTADO DEL SISTEMA*\n\n` +
                      `⏱️ *Uptime proceso:* ${s.uptimeProcesoMin} min\n\n` +
                      `*Proceso (Node):*\n` +
                      `  📦 RSS: ${s.proceso.rssMB} MB\n` +
                      `  🧠 Heap usado: ${s.proceso.heapUsadoMB} / ${s.proceso.heapTotalMB} MB\n\n` +
                      `*Sistema (VPS):*\n` +
                      `  💾 RAM usada: ${s.sistema.ramUsadaPorcentaje}%\n` +
                      `  ⚙️ CPU load (1/5/15 min): ${s.sistema.cpuLoad1min} / ${s.sistema.cpuLoad5min} / ${s.sistema.cpuLoad15min}\n` +
                      `  🔢 Núcleos: ${s.sistema.nucleos}`
            }, { quoted: mensaje })
        }
    },

    // Ops: resumen de actividad de las últimas 24h (o las que se pidan)
    'resumenactividad': {
        async ejecutar(sock, mensaje, args) {
            const jid = mensaje.key.remoteJid
            const horas = parseInt(args[0]) || 24
            const r = await obtenerResumenActividad(horas)
            await sock.sendMessage(jid, {
                text: `📊 *RESUMEN DE ACTIVIDAD (${r.horas}h)*\n\n` +
                      `🤖 *Comandos ejecutados:* ${r.comandosEjecutados}\n` +
                      `👤 *Usuarios únicos:* ${r.usuariosUnicos}\n` +
                      `👥 *Grupos activos:* ${r.gruposActivos}\n` +
                      `❌ *Errores registrados:* ${r.errores}`
            }, { quoted: mensaje })
        }
    },

    // Mantenimiento: activar/desactivar mantenimiento programado
    'mantenimiento': {
        async ejecutar(sock, mensaje, args) {
            const jid = mensaje.key.remoteJid
            const accion = (args[0] || '').toLowerCase()

            if (accion === 'on' || accion === 'activar') {
                const motivo = args.slice(1).join(' ') || 'Mantenimiento programado'
                await activarMantenimiento(motivo)
                await sock.sendMessage(jid, { text: `🔧 *Modo mantenimiento ACTIVADO.*\n\n📋 *Motivo:* ${motivo}\n\nSolo los owners podrán usar comandos hasta desactivarlo con *.mantenimiento off*.` }, { quoted: mensaje })
                return
            }

            if (accion === 'off' || accion === 'desactivar') {
                await desactivarMantenimiento()
                await sock.sendMessage(jid, { text: `✅ *Modo mantenimiento DESACTIVADO.* El bot vuelve a operar con normalidad.` }, { quoted: mensaje })
                return
            }

            const estado = await estaEnMantenimiento()
            await sock.sendMessage(jid, {
                text: `🔧 *ESTADO DE MANTENIMIENTO*\n\n${estado.activo ? `🔴 Activo\n📋 Motivo: ${estado.motivo}` : '🟢 Inactivo'}\n\n💡 Usa *.mantenimiento on <motivo>* o *.mantenimiento off*`
            }, { quoted: mensaje })
        }
    },

    // Emergencia: para todo el bot al instante (excepto owners)
    'emergencia': {
        async ejecutar(sock, mensaje, args) {
            const jid = mensaje.key.remoteJid
            const accion = (args[0] || 'on').toLowerCase()

            if (accion === 'off' || accion === 'desactivar') {
                await desactivarEmergencia()
                await sock.sendMessage(jid, { text: `✅ *Modo emergencia DESACTIVADO.*` }, { quoted: mensaje })
                return
            }

            const motivo = args.slice(accion === 'on' ? 1 : 0).join(' ') || 'Activado manualmente por un owner'
            await activarEmergencia(motivo)
            await sock.sendMessage(jid, {
                text: `🚨 *¡MODO EMERGENCIA ACTIVADO!*\n\n📋 *Motivo:* ${motivo}\n\nTodos los comandos quedan bloqueados para usuarios normales de inmediato.\n\n💡 Usa *.emergencia off* para reanudar.`
            }, { quoted: mensaje })
        }
    },

    // Apagado seguro: deja de aceptar comandos nuevos antes de reiniciar/apagar
    'apagar': {
        async ejecutar(sock, mensaje) {
            const jid = mensaje.key.remoteJid
            await activarApagadoSeguro()
            await sock.sendMessage(jid, {
                text: `🛑 *Apagado seguro activado.*\n\nEl bot dejará de aceptar comandos nuevos. Una vez que la cola de mensajes pendientes se vacíe, puedes apagar el proceso con confianza (\`pm2 stop nexo-bot\`).\n\n💡 Para cancelar, usa *.mantenimiento off* (también limpia el apagado seguro).`
            }, { quoted: mensaje })
        }
    },

    // Rampa de activación gradual: para usar justo después de reconectar
    // el bot tras un período de inactividad o ban previo.
    'activarrampa': {
        async ejecutar(sock, mensaje, args) {
            const jid = mensaje.key.remoteJid
            const accion = (args[0] || '').toLowerCase()

            if (accion === 'off' || accion === 'desactivar') {
                await desactivarRampa()
                await sock.sendMessage(jid, { text: `✅ *Rampa de activación gradual DESACTIVADA.* El bot ya no limita la expansión a grupos nuevos.` }, { quoted: mensaje })
                return
            }

            if (accion === 'on' || accion === 'activar') {
                await activarRampa()
                await sock.sendMessage(jid, {
                    text: `🐢 *Rampa de activación gradual ACTIVADA.*\n\n` +
                          `El bot limitará cuántos grupos NUEVOS puede atender por hora, subiendo el límite progresivamente:\n\n` +
                          `  • Hora 0–1: 3 grupos/hora\n` +
                          `  • Hora 1–3: 8 grupos/hora\n` +
                          `  • Hora 3–6: 20 grupos/hora\n` +
                          `  • Hora 6–12: 50 grupos/hora\n` +
                          `  • Hora 12–24: 120 grupos/hora\n` +
                          `  • Tras 24h: sin límite\n\n` +
                          `Los grupos ya atendidos dentro de una hora siguen funcionando normal; lo que se limita es la velocidad de expansión a grupos nuevos.\n\n` +
                          `💡 Usa *.activarrampa off* para desactivarla antes de tiempo, o *.activarrampa estado* para ver el progreso.`
                }, { quoted: mensaje })
                return
            }

            const estado = await estadoRampa()
            if (!estado.activa) {
                await sock.sendMessage(jid, { text: `🐢 *Rampa inactiva.*\n\n💡 Usa *.activarrampa on* para activarla (recomendado justo después de reconectar el bot).` }, { quoted: mensaje })
                return
            }
            await sock.sendMessage(jid, {
                text: `🐢 *ESTADO DE LA RAMPA*\n\n` +
                      `⏱️ *Horas desde activación:* ${estado.horasTranscurridas}h\n` +
                      `📊 *Límite actual:* ${estado.limiteGruposPorHoraActual} grupos nuevos/hora\n` +
                      `👥 *Grupos atendidos esta hora:* ${estado.gruposAtendidosEstaHora}`
            }, { quoted: mensaje })
        }
    },
    // Reputación de grupos: ver estado de un grupo específico
    'repgrupo': {
        async ejecutar(sock, mensaje, args) {
            const jid = mensaje.key.remoteJid
            const jidConsultado = args[0] || jid

            if (!jidConsultado.endsWith('@g.us')) {
                await sock.sendMessage(jid, { text: `❌ Debes indicar el JID de un grupo, o usar el comando dentro del grupo a consultar.` }, { quoted: mensaje })
                return
            }

            const rep = await obtenerReputacion(jidConsultado)
            await sock.sendMessage(jid, {
                text: `📊 *REPUTACIÓN DE GRUPO*\n\n` +
                      `📍 *Grupo:* ${rep.nombre || jidConsultado}\n` +
                      `📈 *Puntaje:* ${rep.puntaje}/150\n` +
                      `⚠️ *Incidentes:* ${rep.incidentes}\n` +
                      `🕓 *En período de gracia:* ${rep.en_periodo_gracia ? 'Sí' : 'No'}` +
                      (rep.en_periodo_gracia ? `\n📅 *Desde:* ${new Date(rep.gracia_desde).toLocaleString('es-CO')}` : '')
            }, { quoted: mensaje })
        }
    },

    // Reputación de grupos: reportar manualmente un grupo problemático
    'reportargrupo': {
        async ejecutar(sock, mensaje, args) {
            const jid = mensaje.key.remoteJid
            const motivo = args.join(' ') || 'Reportado manualmente por un owner'

            if (!jid.endsWith('@g.us')) {
                await sock.sendMessage(jid, { text: `❌ Este comando se usa DENTRO del grupo que quieres reportar.` }, { quoted: mensaje })
                return
            }

            await registrarIncidente(jid, 'reporte_owner', motivo)
            await sock.sendMessage(jid, {
                text: `⚠️ *Grupo reportado.*\n\n📋 *Motivo:* ${motivo}\n\nLa reputación de este grupo bajó. Si se mantiene crítica por mucho tiempo, el bot podría salir automáticamente.`
            }, { quoted: mensaje })
        }
    },
}

const permitidosEnPrivado = ['addbot']

function obtenerTexto(mensaje) {
    const m = mensaje.message
    return (
        m?.conversation ||
        m?.extendedTextMessage?.text ||
        m?.imageMessage?.caption ||
        m?.videoMessage?.caption ||
        ''
    ).trim()
}

async function cobrarImpuestoSilencioso(userJid) {
    try {
        const [rows] = await db.execute('SELECT monedas FROM usuarios WHERE jid = ?', [userJid])
        if (rows.length === 0 || !rows[0].monedas || rows[0].monedas <= 0) return
        const impuesto = Math.floor(rows[0].monedas * 0.001)
        if (impuesto > 0) {
            await db.execute('UPDATE usuarios SET monedas = monedas - ? WHERE jid = ?', [impuesto, userJid])
        }
    } catch {}
}

let sockGlobal = null
let antibaneoListo = false

export async function manejarMensaje(sock, mensaje) {
    sockGlobal = sock

    // Engancha el sistema anti-baneo en sock.sendMessage una sola vez,
    // apenas tengamos el primer sock activo de la sesión. También
    // activa el sistema de operaciones (monitoreo + anti-crash).
    if (!antibaneoListo) {
        inicializarAntibaneo(sock)
        await inicializarOps(sock)
        antibaneoListo = true
    }

    const jid = mensaje.key.remoteJid
    const esGrupo = jid?.endsWith('@g.us')

    if (esGrupo) {
        await autoRegistrar(sock, mensaje)
    }

    // Detectar imagen, sticker o video → desequipar capa_sigilo
    const tipoMensaje = Object.keys(mensaje.message || {})[0]
    if (esGrupo && ['imageMessage', 'stickerMessage', 'videoMessage'].includes(tipoMensaje)) {
        const userJidMedia = mensaje.key.participant || mensaje.key.remoteJid
        try {
            const [tieneCapa] = await db.execute(
                'SELECT * FROM items_activos WHERE jid = ? AND item = "capa_sigilo" AND expira > NOW()',
                [userJidMedia]
            )
            if (tieneCapa.length > 0) {
                await db.execute('UPDATE inventario_usuario SET equipado = 0 WHERE jid = ? AND item = "capa_sigilo"', [userJidMedia])
                await db.execute('DELETE FROM items_activos WHERE jid = ? AND item = "capa_sigilo"', [userJidMedia])
                await db.execute(
                    'INSERT INTO cooldowns (jid, tipo, expira) VALUES (?, ?, DATE_ADD(NOW(), INTERVAL 15 MINUTE)) ON DUPLICATE KEY UPDATE expira = DATE_ADD(NOW(), INTERVAL 15 MINUTE)',
                    [userJidMedia, 'equipar_capa_sigilo']
                )
                const [invCapa] = await db.execute(
                    'SELECT COALESCE(nivel_mejora, 0) as nivel_mejora FROM inventario_usuario WHERE jid = ? AND item = "capa_sigilo"',
                    [userJidMedia]
                )
                const nivel = invCapa[0]?.nivel_mejora || 0
                const cooldownSecs = Math.max(0, 900 - (nivel * 10))
                const cooldownMin = Math.floor(cooldownSecs / 60)
                const cooldownSec = cooldownSecs % 60
                await sock.sendMessage(jid, {
                    text: `🧥 *¡Capa de Sigilo desequipada!*\n\n@${userJidMedia.split('@')[0]} envió multimedia y su capa fue detectada.\n\n⏳ Cooldown: *${cooldownMin}m ${cooldownSec}s* para volver a equiparla.`,
                    mentions: [userJidMedia]
                })
            }
        } catch {}
        return
    }

    const texto = obtenerTexto(mensaje)
    if (!texto.startsWith(PREFIJO)) return

    const partes = texto.slice(PREFIJO.length).trim().split(/\s+/)
    const cmd = partes[0].toLowerCase()
    const args = partes.slice(1)

    if (!esGrupo && !permitidosEnPrivado.includes(cmd)) {
        await sock.sendMessage(jid, {
            text: `⚠️ *Este bot solo funciona en grupos.*\n\nPara añadirlo a tu grupo usa:\n  ✦ *.addbot <link del grupo>* (sin las <>)\n\n  📌 Ejemplo:\n  *.addbot https://chat.whatsapp.com/xxxxxx*\n\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━`
        }, { quoted: mensaje })
        return
    }

    const comando = comandos[cmd]
    if (!comando) return

    const userJid = mensaje.key.participant || mensaje.key.remoteJid
    const esOw = await esOwner(userJid)

    // ── MANTENIMIENTO / EMERGENCIA / APAGADO SEGURO Y ANTI-BANEO ──
    if (!esOw) {
        const mant = await estaEnMantenimiento()
        if (mant.activo) {
            console.log(`[SILENCIO] Comando .${cmd} de ${userJid} ignorado: Mantenimiento activo.`)
            return
        }

        if (await estaEnApagadoSeguro()) {
            console.log(`[SILENCIO] Comando .${cmd} de ${userJid} ignorado: Apagado Seguro activo.`)
            return
        }

        if (esGrupo) {
            const rampa = await verificarRampa(jid)
            if (!rampa.permitido) {
                console.log(`[SILENCIO] Comando .${cmd} de ${userJid} ignorado: Rampa bloquea el grupo ${jid}.`)
                return
            }
        }

        const flood = verificarFlood(userJid, esGrupo ? jid : null)
        if (flood.bloqueado) {
            console.log(`[SILENCIO] Comando .${cmd} de ${userJid} ignorado: Anti-Flood (${flood.motivo}).`)
            if (flood.motivo === 'limite_hora' || flood.motivo === 'spam_repetido') {
                await sock.sendMessage(jid, {
                    text: `⏳ Estás usando comandos muy rápido. Espera *${flood.minutosRestantes} min* antes de continuar.`
                }, { quoted: mensaje })
            }
            return
        }

        if (verificarSpamInterno(userJid, cmd, args)) {
            console.log(`[SILENCIO] Comando .${cmd} de ${userJid} ignorado: Spam interno repetido.`)
            await sock.sendMessage(jid, {
                text: `🚫 *Comando repetido detectado.*\n\nEspera *1 minuto* antes de volver a usar *.${cmd}* con los mismos parámetros.`
            }, { quoted: mensaje })

            if (esGrupo) {
                registrarIncidente(jid, 'spam_interno', `Comando repetido: .${cmd}`)
            }
            return
        }
    }
    // ────────────────────────────────────────────────────────────────

    // Verificar ban
    try {
        const [baneado] = await db.execute('SELECT id FROM usuarios_baneados WHERE jid = ?', [userJid])
        if (baneado.length > 0) return // ignorar silenciosamente
    } catch {}

    // Verificar mute
    try {
        const [muteado] = await db.execute('SELECT id FROM usuarios_muteados WHERE jid = ?', [userJid])
        if (muteado.length > 0) return // ignorar silenciosamente
    } catch {}

    // Verificar si es comando de owner
    if (COMANDOS_OWNERS.includes(cmd)) {
        if (!esOw) {
            await sock.sendMessage(jid, { text: `🚫 *Solo owners.*` }, { quoted: mensaje })
            return
        }
    }

    await cobrarImpuestoSilencioso(userJid)

    try {
        await comando.ejecutar(sock, mensaje, args)

        registrarActividad({ tipo: 'comando', jid: userJid, chatJid: jid, detalle: cmd })

        // Actividad normal y exitosa en un grupo contribuye lentamente
        // a su reputación (tiene cooldown interno de 6h, no se recalcula
        // realmente en cada comando, es una llamada barata).
        if (esGrupo) {
            registrarActividadSana(jid)
        }

        // Tras comandos que modifican el perfil del usuario (o el de un
        // mencionado), invalida su caché para que .perfil no muestre
        // datos viejos durante la ventana de TTL.
        if (COMANDOS_QUE_INVALIDAN_PERFIL.has(cmd)) {
            const mencionado = mensaje.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]
            invalidarCache(`perfil:${userJid}`)
            if (mencionado) invalidarCache(`perfil:${mencionado}`)
        }
    } catch (error) {
        console.error(`Error ejecutando el comando "${cmd}":`, error)
        registrarError({ comando: cmd, jid: userJid, chatJid: jid, error })
    }
}

// Iniciar verificación de expiraciones cada 5 minutos
export function iniciarVerificacionExpiraciones(sock) {
    setInterval(() => verificarExpiraciones(sock), 5 * 60 * 1000)
}

// Iniciar evaluación periódica de reputación de grupos (cada 30 min).
// No se ejecuta en cada mensaje a propósito: es una tarea de
// mantenimiento de fondo, no una verificación de tiempo real.
export function iniciarEvaluacionReputacionGrupos(sock, ownersJid = OWNERS_JID_GLOBAL) {
    setInterval(() => evaluarTodosLosGrupos(sock, ownersJid), 30 * 60 * 1000)
}